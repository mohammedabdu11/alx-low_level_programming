#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int b;

	int c;

	b = 'a';
	c = 'A';
	while
		(b <= 'z') {
			putchar(b);
			b++;
		}
	while
		(c <= 'Z') {
			putchar(c);
			c++;
		}
	putchar('\n');
	return (0);
}
