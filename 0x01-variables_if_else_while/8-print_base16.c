#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	char a;

	int i;

	a = 'a';
	i = 0;
	while
		(i < 10) {
			putchar(i + '0');
			i++;
		}
	while
		(a <= 'f') {
			putchar(a);
			a++;
		}
	putchar('\n');
	return (0);
}
